docker --version
sudo apt update
sudo apt install apt-transport-https ca-certificates curl software-properties-common -y
sudo systemctl start docker
sudo systemctl enable docker
sudo docker run hello-world
mkdir my-python-app
cd my-python-app
nano app.py
nano Dockerfile
nano requirements.txt
docker build -t myapp-image .
docker run -d -p 5000:5000 --name myapp myapp-image
ls


#Dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY app.py .
EXPOSE 5000
CMD ["python", "app.py"]

from flask import Flask
app = Flask(__name__)
@app.route("/")
def home():
 return "Hello! Docker is running this Python app successfully "
if __name__ == "__main__":
 app.run(host="0.0.0.0", port=5000)
#requirements.txt
flask


docker login -u ankith05
docker tag myapp-image ankith05/my-python-app:latest
docker push ankith05/my-python-app:latest
git config --global user.name "Ankith0505"
git config --global user.email "ankithshetty0505@gmail.com"
git config --list
git clone https://github.com/Ankith0505/jenkins-docker.git
cd jenkins-docker
docker build -t my-python-app .
docker images
